echo '[build]
builder = "RAILPACK"

[deploy]
startCommand = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"' > railway.tomlstartCommand = "streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.fileWatcherType none"
